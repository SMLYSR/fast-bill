## 目标与差异
- 按原型完整还原“历史与统计”页面（顶部月历卡片 + 下方折线图卡片），基线宽度 393px，圆角、阴影、间距、字号需一致。
- 日历：支持左右切月；展示每日数值（红/绿/灰），并保留“无”状态；布局为 7 列网格，行距与圆形日单元尺寸匹配原型。
- 折线图：顶部分段控件（周/月/年）与收入/支出开关；图表带数据点、虚线网格、左侧 y 轴刻度；底部显示本周总支出与日均。

## 组件结构（优先修改现有文件，必要时内联子组件）
- 在 `app/(tabs)/statistics.tsx` 内拆分为两个内联组件：`CalendarCard` 与 `LineChartCard`，减少新文件创建。
- 页面结构：
  - 标题“历史与统计”（居中/间距按原型）
  - `CalendarCard`（白底圆角24，阴影，padding 24）
  - `LineChartCard`（白底圆角24，阴影，padding 24，顶部分段与开关，图表，底部统计）

## 日历卡片实现
- 容器：宽随屏幕，最大 393px；白底，圆角 24，`boxShadow`(web)/Shadow(native)。
- 头部：左右箭头按钮 + 当前月份文案（如“2025年11月”），左右按钮尺寸与间距 20px 图标、点击切换 `monthOffset`。
- 周标题行：['日','一','二','三','四','五','六']，颜色 `#6A7282`，字号 12。
- 网格：使用 `eachDayOfInterval(startOfMonth, endOfMonth)`；单元格 38×38，圆角 14，背景 `#F9FAFB`，居中垂直布局：
  - 上：日期数字（字号 12，颜色 `#1E2939`）
  - 下：数值标签（字号 10，颜色依据类型：支出红 `#FF3B30`、收入绿 `#34C759`、无灰 `#99A1AF`）
- 数据：构造 `dailyStats: Record<string, { type:'income'|'expense'|'none'; value:number|string }>` 的 mock，格式化显示 `+4955`、`-234`、`无`。
- 交互：点击某日可跳转历史详情（保留现有路由），移动端点击区域不重叠。

## 折线图卡片实现
- 顶部控件：
  - 分段（周/月/年）：圆角 9999 胶囊，浅灰底 `#F3F4F6`，内按钮白底；当前选中加阴影；字号 14，字重 500。
  - 收入/支出：并排小胶囊，收入绿色底、支出红色底，字号 12。
- 图表：
  - 使用 `react-native-gifted-charts`；高度约 256；曲线颜色按类型（收入 `#34C759`、支出 `#FF3B30`）。
  - 数据点半径 6，高亮 8；显示虚线网格 `rulesType: 'dashed'`，网格颜色 `#eee`。
  - y 轴刻度显示 `0, 150, 300, 450, 600`；x 轴标签为 `周一`~`周日`。
- 底部统计行：
  - 左：“本周总支出”+ 红色金额（`¥2180.00`）大字号 24；
  - 右：“日均”+ 金额（`¥311.43`）字号 18；文案颜色与原型一致。
- 数据聚合：
  - 以现有交易列表进行 `groupBy weekday` + `sum`（或 mock 数据），切换收入/支出与周期时重新计算。

## 移动端适配
- 使用 `useWindowDimensions()`，内容宽度 `Math.min(393, width - 32)`；手机保持 16px 外边距；平板加至 24px。
- 所有按钮使用跨平台点击封装，避免 Web 事件警告。
- 阴影：原生沿用 `shadow*`，Web 用 `boxShadow`（如 `0 10px 15px -3px rgba(0,0,0,0.1)` 与 `0 4px 6px -4px rgba(0,0,0,0.1)`）。

## 动效与交互
- 月份切换淡入 200ms（简单透明度动画）；点击某日进入历史详情（保留现有 `/history/[date]`）。
- 分段/收入支出切换 150ms 轻微缩放或颜色过渡即可，保证响应。

## 数据与格式
- 金额格式 `¥2180.00`；负数红色，正数绿色；“无”居中显示。
- 保留现有 `formatDateISO` 与 store 计算；若不足，先以 mock 保障 UI，还原后再接入真实数据。

## 验收清单
- 卡片圆角、阴影、间距、字体与颜色与原型一致。
- 日历 7 列、日期圆形背景、下方数值彩色标注；左右切月正常。
- 折线图刻度、虚线网格、数据点与颜色统一；顶部控件与底部统计文案对齐。
- 手机 375px 基线与平板宽度下布局舒适，无溢出或遮挡。

请确认以上方案，确认后我将直接在 `app/(tabs)/statistics.tsx` 内实现这两个子组件并连接现有路由与数据。