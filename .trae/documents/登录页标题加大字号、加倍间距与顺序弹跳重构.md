## 目标
- 将“每日一记”标题字号增大、字符间距翻倍，并把动效重构为始终从左到右依次丝滑跳跃（连续顺序、不重叠）。

## 字体与间距调整
- 移动端：标题 `fontSize: 48px`、`lineHeight: 56px`；平板：`56px/64px`。
- 字符间距翻倍：将 `letterSpacing` 从当前约 `0.5px` 调整为 `1px`；同时在每个字符右侧增加 `marginRight`（如 `8px → 16px`）以保证中文在不同平台视觉一致。
- 图标尺寸保持 40px，标题与图标间距维持 10–12px。

## 动效重构（顺序丝滑）
- 用单一编排循环替代“每个字符独立循环”，确保“左→右”严格顺序：
  - 为每个字符使用 `Animated.Value`（translateY），关键帧：`0 → -10 → 0`。每字符 500ms。
  - 使用 `Animated.stagger(100, perCharSequence[])` 构建一次“左到右”的整组序列；组尾追加 `Animated.delay(2000)` 作为循环间隔。
  - 外层使用 `Animated.loop(groupSequence)`，保证始终按顺序循环；缓动采用 `Easing.inOut(Easing.quad)`（丝滑但不弹性）。
- 保留进入淡入与图标 `scale 0→1 + rotate -180°→0°`（600ms，延迟 400ms）。
- 在“减少动态效果”开启时，仅执行淡入，不做位移/旋转。

## 代码改动点
- `app/(auth)/login.tsx`
  - 更新 `brandText` 的 `fontSize/lineHeight/letterSpacing` 与字符 `marginRight`。
  - 替换当前逐字 `Animated.loop` 为一次性 `Animated.stagger + Animated.loop` 的编排。
  - 保留现有淡入与图标动效逻辑；维持响应式断点（375/768）。

## 验证
- 浏览器打开 `/(auth)/login`：
  - 标题在移动端显示为 48px、字符间距翻倍，左到右严格依次跳跃；循环间隔约 2s。
  - 平板断点为 56px/64px，间距保持加倍效果；图标尺寸与间距正确。
  - “减少动态效果”时仅淡入，排版与色彩不变。

## 备注
- 若加倍间距在某平台对中文显示不稳定，`letterSpacing 1px + marginRight 16px` 双策略并用以保持视觉一致性。

请确认以上方案，我将按此实现并提交修改。