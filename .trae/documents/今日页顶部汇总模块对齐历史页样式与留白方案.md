## 目标
- 今日流水页顶部的汇总模块视觉与历史页一致。
- 模块整体在上下左右留出合理空间，使内容更呼吸、信息层次清晰。

## 对齐点（参考历史页）
- 在历史页中 `SummaryCard` 外包裹 `headerPad: { paddingTop: 12 }`，卡片本身 `marginHorizontal: 16`、`padding: 16`。
- 标题行（日期）在卡片下方，`paddingHorizontal: 16`、`paddingTop: 12`。

## 修改方案（今日页）
1. 在 `app/(tabs)/index.tsx` 的 `ListHeaderComponent` 中：
   - 将 `SummaryCard` 包裹在 `<View style={styles.headerPad}>` 内。
   - 新增 `styles.headerPad = { paddingTop: 12 }`，与历史页保持一致。
2. 保持 `SummaryCard` 的 `marginHorizontal: 16` 与内部 `padding: 16`，确保左右留白统一。
3. 保留现有 `headingRow` 的 `paddingHorizontal: 16` 与 `paddingTop: 12`，与历史页一致，使汇总模块整体“稍微下移”。

## 验证
- Web 与移动端：模块上下左右留白一致；窗口缩放与不同屏幕宽度下布局稳定。
- 与历史页并排对比：卡片与标题间距相同、视觉层级一致。

如确认，我将按上述方案更新 `index.tsx` 并在预览中核验视觉与间距。