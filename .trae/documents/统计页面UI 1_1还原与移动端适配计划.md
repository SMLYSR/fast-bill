## 改动范围
- 仅修改 `app/(tabs)/statistics.tsx` 中已实现的 `CalendarCard` 与 `LineChartCard` 的样式与细节数值，使之与原型 1:1 对齐。
- 不新增文件；继续沿用现有跨平台点击与图表方案，保证 Web/原生一致。

## 视觉规范（按原型）
- 页面宽度：基线 393px；容器左右外边距 16px；卡片最大宽度 `Math.min(393, width - 32)`。
- 卡片：白底、圆角 24、阴影 `0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -4px rgba(0,0,0,0.1)`；内边距 24。
- 标题：居中“历史与统计”，字号 20，颜色 `#101828`，字重 500。

## 日历卡片（CalendarCard）
- 头部：左右箭头按钮 36×36、圆角 18、底色 `#F3F4F6`；月份文案字号 16、字重 500、颜色 `#0B0B0F`；按钮图标大小 20。
- 周标题：`['日','一','二','三','四','五','六']` 字号 12、颜色 `#6A7282`；行与网格间距 4。
- 网格：7 列；单元外容器 `width: 14.285%`、垂直居中、`paddingVertical: 6`。
- 日期圆：38×38、圆角 14、底色 `#F9FAFB`；
  - 数字：字号 12、颜色 `#1E2939`；
  - 数值：字号 10、`marginTop: 2`；支出红 `#FF3B30`（前缀 `-`）、收入绿 `#34C759`（前缀 `+`）、无灰 `#99A1AF` 文案“无”。
- 切月交互：左右箭头切换 `monthOffset`；点击日期跳转 `/history/[date]`。

## 折线图卡片（LineChartCard）
- 顶部分段胶囊：外层 `#F3F4F6`、圆角 9999、padding 4、最小宽 150；
  - 内按钮白底、圆角 9999、`paddingVertical: 3`、`paddingHorizontal: 15`；选择态加轻微阴影；文案字号 14、字重 500、颜色 `#0A0A0A`；文案“周/月/年”。
- 收入/支出开关：右侧并排，圆角 9999；收入 `#34C759`、支出 `#FF3B30`；字号 12、文字白色；选择态加轻微阴影。
- 折线图：高度约 256；线宽 3；数据点半径 6、高亮 8；虚线网格 `rulesType: 'dashed'`、颜色 `#eee`；y 轴文字 `#999`；x 轴文字 `#999`。
- 底部统计行：高度 77，顶部边线 `#F3F4F6`；
  - 左：标签“本周总支出”（12、`#6A7282`），值 `¥2180.00`（24、红色）；
  - 右：标签“日均”（12、`#6A7282`），值 `¥311.43`（18、`#364153`）。

## 数据与格式
- 继续使用现有系列数据计算总计与日均；不足时以示例值填充，保持两位小数。
- 月历每日数值使用 mock，对应原型（如 1:-45、2:-120、8:+4955、14:+200 等）。

## 实施步骤
1. 在 `statistics.tsx` 中将 `CalendarCard` 的样式数值精确调整（圆角、尺寸、颜色、间距、阴影）。
2. 调整 `LineChartCard` 顶部控件排版与视觉数值，更新图表参数（点半径、网格线、文字颜色）。
3. 优化移动端宽度与间距，确保不溢出与对齐正常。
4. 自测在 375px~414px 设备下，核对原型：间距、圆角、字号、色彩、阴影；交互点击区域准确。

## 验收
- 视觉元素（圆角、阴影、字号、颜色、间距）与原型一致；
- 日历 7 列圆形单元、数值彩色标注、切月正常；
- 折线图控件与曲线、网格、底部统计完全对齐；
- 移动端布局无溢出、交互顺畅。