## 差异点
- 卡片阴影不够“突出”，圆角与边距值与原型有偏差
- 日历圆形日单元缺少轻阴影，部分文案（“—/无”）与原型不一致
- 折线图坐标未固定到 0–600、间隔 150，x 轴缺少中文周标签；周数据总计非 2180.00
- 分段胶囊与收入/支出开关的阴影与间距需加强以贴近原型

## 修正项
1) 阴影与圆角
- 在 `app/(tabs)/statistics.tsx` 的 `styles.card` 增加双层阴影（原生两层 Shadow + Web `boxShadow`），保持圆角 24、内边距 24；卡片间距 24
- 为 `dayCircle` 加弱阴影（原生 `shadowOpacity:0.06`、`shadowRadius:2`，Web `boxShadow:'0 1px 2px rgba(0,0,0,0.06)'`）

2) 日历文案与布局
- 保持 7 列，圆形 38×38；日期数字 12 `#1E2939`，下方数值 10、颜色按类型
- 修正月数据 mock 以完全匹配原型：
  - `1:-45`、`2:-120`、`3:'—'`、`4:'无'`、`5:-89`、`6:'无'`、`7:'无'`、`8:+4955`、`9:-234`、`10:'无'`、`11:'无'`、`12:-56`、`13:'无'`、`14:+200`、`15:-145`、`16:'无'`、`17:-67`、`18:'无'`、`19:-234`、`20:-234`、`21:'无'`、`22:-89`、`23:'无'`、`24:-156`、`25:'无'`、`26:-78`、`27:-234`、`28:-234`、`29:-234`、`30:-123`、`31:'无'`
- “—” 使用灰色 `#99A1AF`，与“无”同色但字符不同

3) 折线图 1:1
- 设置 `LineChart`：`maxValue=600`、`noOfSections=4`、线宽 3、点半径 6/高亮 8、虚线网格 `#eee`、坐标文字 `#999`
- 加 `xAxisLabelTexts=['周一','周二','周三','周四','周五','周六','周日']`（库支持时启用）；否则用底部自定义标签
- 周数据改为原型大数据，总计 2180.00：`[150,300,450,150,320,520,290]`；底部统计显示 `本周总支出 ¥2180.00` 与 `日均 ¥311.43`

4) 顶部控件与开关
- 分段胶囊与收入/支出开关选中态增加轻阴影；内外间距按原型微调（外层 padding 4、内按钮 `px:15`）

## 验收
- 卡片阴影明显、圆角与内边距精确；日历“—/无”与数值颜色完全一致；折线图坐标与周标签、曲线形状、总计与日均与原型一致；移动端 393px 下无溢出

确认后我将直接在 `app/(tabs)/statistics.tsx` 完成上述修正并回传更新点。