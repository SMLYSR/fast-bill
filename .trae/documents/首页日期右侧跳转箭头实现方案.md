## 目标
- 在今日页标题行的日期文本右侧增加一个样式与颜色一致的箭头按钮（">"/Chevron）。
- 点击箭头跳转到“统计”页面的日历模块（统计页顶部即为日历卡片）。

## 改动点
1. 首页文件 `app/(tabs)/index.tsx`
- 在 `ListHeaderComponent` 的日期文本后添加一个 `CrossPressable`，内部使用 `Ionicons` 的 `chevron-forward` 图标。
- 颜色与字号与日期保持一致（颜色 `#101828`，字号 18）。
- 引入并使用 `useRouter`，在 `onPress` 中 `router.push('/(tabs)/statistics')`。

## 验证
- Web 与移动端：点击箭头后切换到统计页，看到顶部日历模块。
- 视觉一致：箭头与日期同一行、同色、同字号，间距适当（如 `marginLeft: 6`）。

如确认，我将按上述方案更新首页并在预览中验证交互。