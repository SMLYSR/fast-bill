## 目标
- 按原型图对“历史流水”页面进行像素级重设：布局、视觉、交互、数据格式与动效完全一致，覆盖 Web、iOS、Android。

## 设计规范
- 配色：`#007AFF` 主色、`#FF3B30` 支出红、`#34C759` 收入绿、文字主色 `#101828`、副文 `#6A7282`、卡片白 `#FFFFFF`、页面背景 `#F5F5F5`。
- 字体：标题/页眉使用圆角族（`Fonts.rounded`，见 `constants/theme.ts:30-53`）；正文使用 `Fonts.sans`；金额加粗。
- 圆角与阴影：卡片圆角 `24`，按钮圆角 `28`；阴影 Web `box-shadow` 与原生 `shadow*` 同步，参考现有样式实现。
- 间距：页面横向间距 `16`；卡片内边距 `16/24`；列表行垂直间距 `10`；元素间 `gap` 严格按原型。

## 改造范围
- 页面：`app/history/[date].tsx`（结构与头部、列表布局重构；参照 `37-45` 的样式段更新）。
- 汇总卡：复用并强化 `components/SummaryCard.tsx`（`35-47`），新增“总收支/总支出/总收入”三列样式与数值颜色规则、左侧钱袋 Emoji 头部。
- 列表项：更新 `components/HistoryItem.tsx`（`68-82`）实现时间、类别、金额右对齐、位置信息与备注的图标+文案行；严格对齐原型的字体与颜色。
- 日期标题：在 `app/history/[date].tsx` 使用 `utils/format.ts:17-24` 的 `formatCNDateWithWeek` 渲染“11月14日(周五)”。
- 滚动与分页：使用 `FlatList` 虚拟化；Web 端注入自定义滚动条样式（仅 Web）。
- 动效：页面进入过渡、卡片与列表项渐显/轻微上移；点击/悬停状态反馈色与阴影。

## 数据与格式
- 数据加载：继续使用 `store/useTransactionStore.ts:20-26` 的 `loadByDate(dateISO)`；统计页跳转已注册 `app/_layout.tsx:31-38`。
- 数值格式：用 `utils/format.ts:1-4` `formatCurrency` 输出金额（在支出前加负号，收入加正号，尾部加 `¥`），并按类型着色（红/绿）。
- 汇总：通过 `sumBy`（`utils/format.ts:13-15`）计算当日总收支、总支出、总收入。

## 实施步骤
- 1. 重构 `app/history/[date].tsx` 布局：
  - 顶部导航区标题“历史流水”，下方 `SummaryCard` 占满行；其后渲染“CN日期+周几”标题；再下方是当日列表。
  - 页面容器背景 `#F5F5F5`，左右边距 `16`，头部上下间距匹配原型。
- 2. 强化 `SummaryCard`：
  - 头部左侧钱袋 Emoji（`Text`），右侧“当日总收/支”副标题；下方三列：标签（灰色）、数值（支出红、收入绿、收支主色），中间用细竖分隔线。
  - 值显示：总收支前缀 `+/-`，支出红 `#FF3B30`，收入绿 `#34C759`。
- 3. 列表项 `HistoryItem`：
  - 左侧圆形背景、中心 Emoji；右侧内容区：
    - 顶部行：左侧时间（灰/斜体）与类别名；右侧金额（20px、按类型着色，右对齐）。
    - 次行：位置与备注各一行带图标（定位、文档），字体 12px、灰色。
  - 行间距与对齐与 `TimelineItem.tsx:41-55` 保持一致但按原型微调。
- 4. Web 滚动条样式：
  - 在 `app/_layout.tsx` 或历史页组件内按平台注入 `<style>`，为 `ScrollView`/`FlatList` 容器 `nativeID="historyList"` 定义 `::-webkit-scrollbar` 与 `scrollbar-width: thin`（Firefox）以匹配原型风格。
- 5. 交互状态：
  - 列表项按压提升阴影或背景轻微变亮；按钮（若存在）含正常/悬停/点击三态；颜色与阴影值与原型一致。
- 6. 动效实现：
  - 进入：`Animated` 渐显 + Y 轴 8px 上移，时长 `240ms`，`Easing.out(Easing.cubic)`。
  - 列表项：stagger 60ms；点击：缩放 `0.98` 并回弹。

## 适配与响应式
- 栅格：移动端 1 列；平板/桌面维持居中最大宽度 393-480；统计页到历史页的联动不变。
- 断点：宽度 ≥ 768 调整卡片内边距与字体；Web `Platform.select({ web: { maxWidth, marginHorizontal: 'auto' } })` 居中。

## 测试与验收
- 视觉比对：选择 iPhone 12（390×844）、Android 360×800、iPad 768×1024、桌面 1440×900 四档；对 Summary 卡、列表行、标题行进行像素检查。
- 状态检查：支出/收入颜色、正负号、单位符号一致；空态文本与加载过渡正确。
- 动效与交互：进入/点击/悬停时长与缓动函数符合原型；滚动条样式在 Web 生效。
- 回归：统计页跳转至历史页（`app/(tabs)/statistics.tsx:73-79`）依旧可用，数据库查询逻辑保持不变。

## 交付物
- 更新 `app/history/[date].tsx`、`components/SummaryCard.tsx`、`components/HistoryItem.tsx`，必要时为 Web 注入滚动条样式（不影响原生）。
- 不改变数据层与 Store 接口；仅进行 UI 改造与动效补充。

请确认方案；确认后我将开始实现并在本地预览中进行多设备视觉比对与动效验证。