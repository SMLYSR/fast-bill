## 目标
- 将 `Minimalist Financial Diary App` 的“我的”页面按照原型图 1:1 还原，高保真视觉与交互。
- 保持移动端完美适配（iOS/Android + Web），不新增依赖。

## 改动范围
- 仅修改 `app/(tabs)/profile.tsx`，不新增文件。

## 设计细节（对齐原型）
- 顶部标题：`个人信息`，左右居中，颜色 `#101828`。
- 资料卡片：白底、圆角 24、强投影；内含圆形渐变头像（蓝→绿）、主标题“每日记账用户”、副标题“user@dailyflow.com”。
- 功能列表：四个条目（编辑资料、系统设置、主题设置、隐私设置）。
  - 左侧圆形浅色图标背景，分别为 pencil、settings、palette、lock。
  - 右侧 `>` Chevron（`IconSymbol`：`chevron.right`）。
  - 列项卡片白底、圆角、分割线与阴影，行高对齐原型。
- 退出登录：底部大按钮，白底+红色 2px 边框，双向阴影（Web boxShadow；原生 shadow*），文字红色加粗。
- 页面整体：背景 `#f5f5f5`、垂直滚动、左右内边距 16，元素宽度适配屏宽（不超过 361px）。

## 技术实现
- 保留现有逻辑：`logout()` 已实现，点击退出后跳转 `/(auth)/login`（已接线）。
- 使用现有依赖：
  - 图标：`@expo/vector-icons/Ionicons`（左侧图标），`IconSymbol`（右侧 chevron）。
  - 渐变圆形：`react-native-svg` 的 `Defs/LinearGradient/Circle`（项目已使用 `react-native-svg`，无需新增包）。
- 自适配：`useWindowDimensions()` 控制卡片宽度 `min(361, width - 32)`；在 Web 端使用 `boxShadow`，原生端使用 `shadow*`。

## 具体步骤
1. 重构页面骨架：包裹为 `ScrollView`，顶部标题与背景色。
2. 实现资料卡片：
   - 左上居中的渐变圆形头像（`Svg` 绘制），下方主/副标题文本；卡片阴影与圆角按原型。
3. 实现四个功能列表项：
   - 公用行样式（高度 81，左右 24 内边距，底部分割线），左侧圆形浅色图标背景，右侧 chevron；点击暂时 `onPress={() => {}}`（后续可接跳转）。
4. 实现退出登录按钮：
   - 2px 红色边框、圆角大号、白底；`boxShadow: '4px 4px 12px rgba(255,59,48,0.2), -4px -4px 12px rgba(255,255,255,0.8)'`（Web），原生 `shadow*`。
5. 适配细节：
   - 文本字号、行高、颜色与间距按照 `.figma/3_4156` 的 `scss` 定义对齐；在移动端调整为像素完美。

## 验证
- Web/移动端分别打开，检查：
  - 卡片宽度、阴影、圆角与间距与原型一致。
  - 列表项图标与 chevron 对齐，点击有触控态（ActiveOpacity/CrossPressable）。
  - 退出登录按钮样式与交互正确，点击返回登录页。

确认后我将直接在 `app/(tabs)/profile.tsx` 完成上述实现并本地验证效果。