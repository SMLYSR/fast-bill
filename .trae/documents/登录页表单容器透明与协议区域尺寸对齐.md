## 目标
- 将输入表单外层容器背景改为透明（保持内容样式与布局）。
- 协议区域保持原颜色，但统一圆角与尺寸为按钮规格（高度 56、圆角 16、宽度与按钮一致）。

## 变更点
- `app/(auth)/login.tsx`
  - 修改 `formCard` 背景为透明，并去除阴影（避免透明背景下阴影失真）。
  - 将 `agreeRow` 调整为与按钮一致的宽度与高度（56），圆角 16，水平内边距 16，垂直居中对齐；保留当前颜色。
  - `agreeRow` 放置于与按钮同一宽度体系（`width: '100%'`，由外层卡片控制实际宽度）。

## 验证
- 浏览器访问 `/(auth)/login`：
  - 输入表单的外层容器不再显示白底，背景透明；输入框仍白底且可读。
  - 协议区域视觉与按钮宽度一致，高度 56、圆角 16、颜色不变；内部内容垂直居中，交互正常。

确认后我将执行代码修改并在预览中验证。