## 目标
- 将登录页标题区域的白色背景改为透明，保留网格线与其它区域的白卡片样式，避免影响可读性。

## 变更范围
- 标题区域容器（brand 区域）所在页面根容器背景改为透明。
- 网格组件的底色由白色改为透明，使网格线在透明背景上展示。
- 保持表单卡片、输入框和按钮的白底与阴影不变。

## 实施步骤
1. 修改 `app/(auth)/login.tsx` 中根容器样式：`wrap.backgroundColor` 从 `#fff` 改为 `transparent`。
2. 更新 `components/GridBackground.tsx`：移除白色底 `Rect` 或将其 `fill` 改为 `transparent`，仅保留网格线绘制。
3. 检查标题与标语对比度：如透底后可读性下降，保持文本颜色（近黑 `#030213`）不变。
4. 验证在移动与平板断点下网格与标题区域透明效果正常，不影响表单白卡片与交互。

## 验证
- 访问 `/(auth)/login`，标题区域背景为透明，网格线可见，表单卡片依旧白底。
- 在“减少动态效果”开启时，透明背景仍正常显示，无额外闪烁。

请确认该方案，确认后我将执行修改并验证。