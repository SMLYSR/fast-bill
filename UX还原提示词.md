**系统指令模板**
- 目标：在移动优先的单页应用中，还原 5 个主要页面的 UI/UX 效果与 10 个关键交互，所有入场/显隐/展开动效统一 `300ms`（登录品牌露出 `600–800ms`），不引用任何现有代码。
- 页面集合：`登录`、`今日流水`、`统计`、`历史流水`、`我的`（共 `5` 个）。
- 视觉规范：主色 `#007AFF`（约 `40%` 用于主操作与高亮）；辅助色 `#FF3B30`（支出）、`#34C759`（收入）、`#FFB800`（提示/总额）；字体为 System UI Sans；字号范围 `10–48px`；字重 `400/500`；圆角偏大；阴影柔和。
- 布局与响应式：移动优先；内容容器居中且窄宽度；底部导航固定；断点 `375px`（移动基准）、`768px`（平板）；日历 `7` 列栅格；滚动时在顶部出现“返回顶部”圆角按钮。
- 交互规则：收入数值带 `+`；货币单位 `¥` 后置；列表项、弹窗、悬浮按钮展开/收起与标签页切换动效均为 `300ms`；点击日期进入历史页；主题切换应用到全局背景与文本。
- 性能与体验：首屏加载 ≤ `2s`；统计与历史视图可懒加载并配合骨架屏；图表按需加载；弱网下动效不影响操作与可读。
- 质量约束：禁用态统一半透明不可点击；激活态明确且一致；点击遮罩关闭弹窗；按钮与状态反馈清晰；颜色与数值表达严格遵循上面规则。

**开发者指令模板**
- 页面拆分与导航
  - 构建 `登录/今日流水/统计/历史流水/我的` 五页；底部导航固定，点击高亮为主色 `#007AFF`；历史页由统计页日期点击进入；返回按钮回到主视图。
- 汇总模块（今日/历史）
  - 三分栏：总收支（黄 `#FFB800`）、总支出（红 `#FF3B30`）、总收入（绿 `#34C759`）；收入数值带 `+`；单位 `¥` 后置；竖向分隔线弱对比；卡片阴影柔和；字号自适配但不跳变。
- 时间线模块
  - 列表项入场为 `opacity+y` 的 `300ms` 动效；左侧圆形底色按类型半透明（收入绿、支出红）；右侧包含时间（可用装饰字形或斜体）、类别、金额；地点与备注作为次要信息行（含小图标）。
- 悬浮操作模块（今日页右下角）
  - 主按钮为圆形主色，点击旋转 `45°` 并展开两枚圆角子按钮：AI 记录（相机图标）与手动记账（笔图标）；弹窗居中淡入；手动记账弹窗含类型切换、金额、时间、类别、地点、备注与“保存”按钮（当前保存为占位，不写入数据）。
- 统计模块
  - 顶部月份标题与左右切换箭头；下方日历标题行为周标；日历为 `7` 列栅格，日期方块内显示当天收/支小数，颜色随类型；无记录显示中性占位符；点击某天进入历史视图。
  - 图表区：周/月/年切换为圆角“胶囊”风格；右侧“收入/支出”按钮切换图表类型与颜色（绿/红）；折线加粗、点半径 `5`、激活点 `7`、网格为浅色虚线；下方汇总显示总额与日均。
- 历史流水模块
  - 顶部固定返回按钮与标题；结构与“今日流水”一致但数据为所选日期；滚动后出现“返回顶部”圆角按钮。
- 我的模块
  - 顶部头像为渐变圆形；入口为卡片化列表（编辑资料/系统设置/主题设置/隐私设置）；主题设置弹窗含浅色/深色/系统三项，当前项右侧勾选；选择后全局主题切换；底部固定“退出登录”圆角按钮（白底红边红文）。
- 动效与样式
  - 全局基础动效 `300ms`；登录品牌露出 `600–800ms`；缓动舒适不弹性；避免影响信息读取。
- 响应式
  - `375px` 基准；`768px` 断点：增加间距与字号；内容保持触控友好。
- 验收风格
  - 每项交互“可见/可感知/一致”；状态“可辨识”；颜色与数值表达准确；动效“不打扰”。

**用户故事与验收模板**
- 底部导航
  - 当我点击“今日/统计/我的”时，当前标签文字和图标高亮为 `#007AFF`，页面内容切换流畅且无跳闪；底部导航在任何页面都固定可见。
- 今日流水
  - 当我滚动离开汇总卡片后，页面顶部中央出现“返回顶部”圆角按钮；点击后平滑滚动到页面顶端（约 `300ms`）。
  - 当我点击页面“日期标题”时，进入“统计”页面；视觉过渡无突兀。
  - 当我点击右下主按钮时，它旋转 `45°` 并在右侧展开两枚圆角子按钮（AI 记录/手动记账）；点击任一子按钮弹出居中的圆角弹窗并淡入；手动记账弹窗包含类型、金额、时间、类别、地点、备注与“保存”，当前“保存”为占位行为（不实际写入）。
- 汇总表达（适用于今日与历史）
  - 我看到三分栏的“总收支/总支出/总收入”，它们的颜色分别是 `#FFB800`、`#FF3B30`、`#34C759`；收入数值前带 `+` 号；所有货币单位 `¥` 后置；竖向分隔线对比度低且不抢视觉。
- 时间线项
  - 每一项左侧为圆形底色（按类型绿/红半透明），右侧包含时间（装饰字形或斜体）、类别、金额；可能包含地点与备注的次要信息行并带小图标；列表项入场为 `300ms` 的淡入下滑。
- 统计视图
  - 当我切换“周/月/年”时，胶囊按钮高亮样式变化且图表按对应周期更新；当我切换“收入/支出”时，图表颜色在绿/红之间切换并且下方汇总数值与日均同步变化。
  - 当我点击日历中的某一天时，将进入该日的“历史流水”页面；无记录的日期显示中性占位符（无颜色提示）。
- 历史流水
  - 页面顶部固定显示返回按钮与“历史流水”标题；结构与“今日流水”一致，但日期为我选择的日期；当我滚动时，顶部下方出现“返回顶部”圆角按钮并在点击后平滑滚动到顶端。
- 我的页面
  - 我看到顶部渐变圆形头像与用户名信息；下方为卡片化入口列表；当我点击“主题设置”时，弹窗显示“浅色/深色/系统”三项，当前选项右侧有勾选；切换后全局背景与文本色立即更新；底部的“退出登录”按钮为白底红边红文的强提示样式。
- 通用动效与体验
  - 所有弹窗、列表项与按钮交互动效时长均为 `300ms`；登录品牌露出 `600–800ms`；动效不会妨碍信息读取与操作；禁用态不可点击且半透明；激活态样式明确且一致。
- 性能与响应式
  - 首屏在常规网络环境下 ≤ `2s`；统计与历史视图首次加载出现骨架占位；`375px` 与 `768px` 下布局与触控尺寸合理。

**生成与交付要求**
- 生成物：五页 UI/UX 实现、统一动效、配色与排版体系、响应式断点与日历栅格、统计图表与汇总一致性、弹窗与悬浮操作模块。
- 约束：不得更改颜色语义或增加新主色；不得省略任一关键交互；不得改变金额符号与单位规则；不得使用过度动效或阻碍可读性。
- 验收：按“用户故事与验收模板”逐条核对；动效统一 `300ms`；颜色/字体/布局/响应式严格遵循规范；统计图与日历点击联动准确。